services:
  mongo:
    restart: always
    image: mongo:8.0.0
    volumes:
      - mongo_data:/data/db
    networks:
      - express-mongo
    expose:
      - 27017
    ports:
      -27017:27017
    attach: false

networks:
  express-mongo:

volumes:
  mongo_data:
